# 引用
引用就是变量的别名，不是真正的变量

```c++
    int a = 5;
    int& ref = a;
```

> [!TIP]
> 引用不能只声明不赋值必须初始化,一旦声明了一个引用,不能更改它所引用的对象。如果想要更改其指向的对象，需要使用指针

> [!IMPORTANT]
> ### 指针与引用的区别
> - 引用不是真正的变量，只是变量的别名,而指针是存放地址的变量。
> - 引用必须初始化且不能更改其指向的对象,指针可以使用0,NULL,Nullptr设置为空指针并且可以改变指向的对象。