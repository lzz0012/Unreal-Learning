## 箭头运算符获取某个成员的偏移量
```c++
struct Vector3
{
    float x, y ,z;
}

int offset = (int)&((Vector3*)0)->y;
```

> [!NOTE]
> - (Vector3*)0 : 将地址0转换成Vector3指针，即假设存在一个从地址0开始的Vector结构体
> - &((Vector3*)0)->y : 获取y的地址，由于基地址从0开始，所以y的地址也是y在结构体中的偏移量