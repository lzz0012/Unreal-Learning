## 构造函数
- 在创建一个实例时运行
- 没有返回值
- 构造函数名称与类名一致
- 类默认存在一个构造函数，这个构造函数不接受参数，函数体也什么都不做
  

> [!NOTE]
> 如果不希望创建类实例
> - 通过private隐藏构造函数
> - 删除构造函数

```c++
class Log
{
private:
    Log(){}

public:
    Log() = delete;
}
```

## 析构函数
- 在销毁实例时运行
- 用于释放内容和清理内存空间
- 和构造函数唯一的区别在于要在前面加上~
